@model IEnumerable<CoverType>
@{
	ViewData["Title"] = "Category Index";
}
<div class="container p-3">
	<div class="row pt-4">
		<div class="col-6"><h1 class="text-primary">Cover Type List</h1></div>
		<div class="col-6">
			<a asp-controller="CoverType" asp-action="Create" class="btn btn-primary"><i class="bi bi-plus-square-fill"></i> &nbsp; Create New Cover Type</a>
		</div>
	</div>
</div>
<br />
<table class="table table-bordered table-striped">
	<thead>
		<tr>
			<th>
				Cover Type Name
			</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td width="50%">
					@item.Name
				</td>
				<td width="50%">
					<div class="w-75 btn-group" role="group">
						<button id="btnGroupDrop1" type="button" class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Actions</button>
						<div class="dropdown-menu" aria-labelledby="btnGroupDrop1">
							<a asp-controller="CoverType" asp-action="Edit" asp-route-id="@item.Id" class=" btn btn-info"><i class="bi bi-pencil-square"></i> &nbsp; Edit</a>
							<a asp-controller="CoverType" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger"><i class="bi bi-trash-fill"></i> &nbsp; Delete</a>
						</div>
					</div>
				</td>
			</tr>
		}
	</tbody>
</table>

